# GPU
GPUtil

# Core numerical / scientific
numpy
scipy

# Geometry / point cloud

open3d
open3d-python
pyvista
pyvistaqt
vtk

# GUI
PySide6

# Geospatial / projections / point cloud formats
laspy
pyproj
geopandas
shapely
laspy[lazrs]

geopandas 
shapely 
fiona 
rasterio
# DXF / CAD IO
ezdxf

# Plotting (used in performance_optimizations elevation colormap)
matplotlib

# Optional: if you use these in code not fully visible here, add as needed
# pandas
# pillow

# ── EXISTING (already in your env, pin for reproducibility) ───────
numpy>=1.26.0,<3.0.0
vtk>=9.3.0
pyvista>=0.44.0
pyvistaqt>=0.11.0
PySide6>=6.7.0
pyproj>=3.6.0
psutil>=6.0.0

# ── NEW: SNT FORMAT PIPELINE ──────────────────────────────────────
ezdxf==1.3.5
zstandard==0.23.0

# ── NEW: C++ BUILD (dev/compile time only) ────────────────────────
pybind11==2.13.6
cmake==3.31.4
scikit-build-core==0.10.7

# ── NEW: TESTING ──────────────────────────────────────────────────
pytest==8.3.4
pytest-benchmark==5.1.0

